import React from "react";

const SearchBar = () => {
  return (
    <div>
      <form onSubmit={handleSubmit}>
        <input type="text" />
      </form>
    </div>
  );
};

export default SearchBar;
