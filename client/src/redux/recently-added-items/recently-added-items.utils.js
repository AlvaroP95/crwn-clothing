export const addRecentlyAddedItem = (recentlyAddedItems, itemToAdd) => {
  return { name: itemToAdd.name, id: itemToAdd.id };
};

// export const removeRecentlyAddedItem = () => {
//   return {};
// };
